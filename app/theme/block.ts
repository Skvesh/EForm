import { ViewStyle } from 'react-native/types';

const alignCenter: ViewStyle = {
    justifyContent: 'center',
    alignItems: 'center',
};

export const block = {
    marginBottom: 20,
    marginRight: 10,
    alignCenter,
};
